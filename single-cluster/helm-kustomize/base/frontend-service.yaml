apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: ticket-sales-web
    tier: frontend
spec:
  type: NodePort
  ports:
  - port: 80
  selector:
    app: ticket-sales-web
    tier: frontend
